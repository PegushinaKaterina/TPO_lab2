import org.openqa.selenium.WebDriver
import org.openqa.selenium.support.ui.WebDriverWait

class JobPreferencesPage(private val driver: WebDriver, private val waits: Map<WebDriver, WebDriverWait>) :
    Page(driver, waits) {



}